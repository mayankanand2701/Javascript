// to change the class properties using js
// originally the class given wa red and text-dark
first.className="yellow text-dark";

// to remove any class form the list of classes given to any tag
first.classList.remove("yellow");

// to add any class
first.classList.add("yellow");

// to alterate the presence
first.classList.toggle("yellow");
// Output : it will remove the color if present if not present if will add the yellow color to it