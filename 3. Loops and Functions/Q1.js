// for loop
// for(let i=0;i<5;i++)console.log(i)

// for in loop 
// it iterate over key of object
const obj={
    Mayank:97,
    Raj:78,
    Kuldeep:100,
    Rakesh:88
}
for(let a in obj)console.log("The marks of "+a+" is : "+obj[a]);

// for of loop
// it iterate over the value of the object
for(let b of "Mayank")console.log(b);
