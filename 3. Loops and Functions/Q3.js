// Functions 
function avg(x,y) 
{
    console.log("Done")
    return 1+(x+y)/2;
}

let a=1;
let b=2;
let c=3;

console.log("One plus the average of a and b is : ",avg(a,b));
console.log("One plus the average of b and c is : ",avg(b,c));
console.log("One plus the average of c and a is : ",avg(c,a));
